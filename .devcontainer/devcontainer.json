{
  "build": { "dockerfile": "Dockerfile" },
  "features": {
    "ghcr.io/devcontainers/features/python:1": { "version": "3.11" }
  },
  "containerEnv": {
    "CLAUDE_CONFIG_DIR": "/home/vscode/.claude",
    "DISPLAY": ""
  },
  "mounts": [
    "source=claude-code-config-${devcontainerId},target=/home/vscode/.claude,type=volume"
  ],
  "postCreateCommand": "bash -lc 'sudo chown -R vscode:vscode ~/.claude; curl -fsSL https://claude.ai/install.sh | bash; echo \"export PATH=\\\"$HOME/.local/bin:$PATH\\\"\" >> ~/.bashrc; python -m pip install -r requirements.txt; godot --headless --version'",
  "remoteUser": "vscode"
}