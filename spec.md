# Ball Collector 3D — 仕様書

Godot 4.4 + godot-rl + Stable-Baselines3 による「人間もプレイでき、AI も強化学習で上達する」3D ゲーム。

## まとめ

### 決まったこと

#### ゲーム内容
- **ボール・コレクター**: 球体を転がしてフィールド上のターゲットを集める 3D ゲーム
- **操作/物理**: `RigidBody3D` + トルク制御（慣性で滑る Marble Madness 系の手触り）
- **カメラ**: 球体を追従、**回転は固定**（画面奥 = ワールド -Z）。人間の操作方向と AI の行動空間の意味を一致させるため
- **ルール**: 60 秒タイムアタック。ターゲット取得で +1 点、場外落下でリスポーン（減点）
- **ターゲット**: 取ると別のランダム位置に再出現（常に一定数 = 観測次元が安定）

#### 実行環境
- **プレイ方法**: X11 転送でウィンドウ実行（`DISPLAY=:4` が VS Code Dev Containers 経由で生きていることを実測確認済み）
- **レンダラ**: `gl_compatibility`。GPU は無く **llvmpipe（CPU ソフトウェア描画）** のため、影オフ・低ポリで軽量に保つ
- 音声は出ない（オーディオドライバ無し）

#### AI（強化学習）
- **アルゴリズム**: Stable-Baselines3 の **PPO**（連続行動）
- **観測**: 12 次元の数値ベクトル（自己速度 3 / 自己位置 3 / 最寄ターゲット相対 2 / 2 番目 2 / 距離 1 / 残り時間 1）。画像は使わない
- **行動**: 連続 2 次元（X/Z トルク、各 [-1, 1]）
- **報酬**: ターゲット取得 +1.0 / 落下 -1.0 / 距離シェーピング（接近で微小加点）
- **並列化**: 1 プロセス内にアリーナを 16 個並べて 16 体同時学習（godot-rl 標準手法）+ プロセス並列は 4 まで
- **スコープ**: 実際に学習を回して報酬が上がることを確認するところまで（構造だけで終わらせない）

### 制約として確定したこと
- **Godot 内 ONNX 推論は不可**。プラグインの推論部は C# 実装で、導入済み Godot は非 .NET ビルド。
  → AI のプレイを見るときは **Python 側が env を駆動し、その画面を X11 で表示**する
- 人間プレイ（ソフトウェア描画）の方が、AI 学習（ヘッドレス）より負荷が高い

### 実測データ（見積もりの根拠）
| 項目 | 実測値 |
|---|---|
| Godot ヘッドレス 1 インスタンス | 85 MB |
| PPO 学習中のピーク RSS | 672 MB |
| PPO スループット（小型 MLP, CPU） | 1,500 steps/秒 |
| 利用可能 CPU / RAM | 12 コア / 7 GB |
| GPU | 無し（CUDA 利用不可） |

### 意図的に見送ったこと（次の一手）
- 障害物 + `RaycastSensor3D` による周辺認識（v1 は平坦なフィールド + 場外落下のみ）
- Fly By 風のゲートくぐりゲームへの発展（同じ骨組みを流用可能）
- Web/Windows へのエクスポート（エクスポートテンプレート 1.2GB のダウンロードが必要）

---

# Fly By 3D — VS モード 計画

2026 年 8 月 19 日、実装前に interview-me で詰めた合意事項。まだ 1 行も書いていない。

ドローンのゲート くぐり (Fly By) に、Ball Collector で作った「学習済み方策をゲーム内に
埋め込んで人間と対戦させる」仕組みを移植する。既存のゲームは両方とも残す。

## まとめ

### 決まったこと

#### 統合方針
- **`worktree-vs-ai-mode` ブランチを master にマージしてから、その上にドローン VS を作る**。
  部品だけ拾う案は却下。Ball Collector の VS モードが master から消えてしまうため
- マージすると **Ball Collector の人間プレイは VS モードになる** (`play.tscn` に
  `rival_enabled = true` が入り、タイトル → カウントダウン → 対戦 → 結果 の構成になる)
- **ドローン VS は `play.gd` を共通化せず、別ファイルとして書く**。
  `play.gd` は Ball Collector 専用に 417 行ぶん作り込まれており、共通化すると両方を壊す

#### レースの形式
- **ゴースト方式**。同じ形のコースを 2 本、同じ座標に重ねる
  - 乱数シードを揃えて生成するので、2 機はまったく同じ経路を飛ぶ
  - 当たり判定はコリジョンレイヤーで分離。相手のリングと地面は非表示
  - 得点は独立。相手は自分の進路の先 (か後ろ) を飛ぶゴーストとして見える
- この方式を選んだのは **`course.gd` の目標ゲート添字まわりを一切触らずに済む**ため。
  1 コース 1 機という現在の前提が保て、シングルプレイと学習パイプラインが壊れない
- 代償として **機体同士の物理的な妨害は無い**。純粋なタイムアタック対決になる

#### AI の手加減
- **速度上限 (主) ＋ 行動ノイズ (従)** の 2 つを `skill` 1 本から導く
  - 通過数はほぼ「距離 ÷ ゲート間隔」で決まる (60 秒 × 速度 ÷ 21.5m) ので、
    速度上限は強さの天井を確実に決められる
  - ガウスノイズで「たまに狙いを外す」質感を足す。EASY ほど強くする
- **旋回角速度は触らない**。ドローンは遅いほど小回りが利くため、
  Ball Collector と同じ「トルクと速度上限を skill 倍」では単純な弱体化にならない
- **`max_speed` (観測の正規化用・不変) と `speed_cap` (手加減用・可変) を別の変数に分ける**。
  観測は速度を `max_speed` で割って正規化しているので、ここを一緒にすると
  手加減が観測の歪みとして方策に伝わる (Ball Collector は正規化に定数 15.0 を
  使っていたのでこの問題が無かった)
- 段階は Ball Collector と同じ 4 つ。プレイ中に 1-4 キーで変更できる

#### 差の見せ方
- **差分を 1 行で表示する**。「+2 ゲート / 1.4 秒リード」の形
  - 同一経路を飛ぶ以上、相手との関係は「経路上でどれだけ前か後ろか」の 1 次元しかない
  - 経路上の進行距離は `course.gd` が既に持つ情報 (通過数 ＋ 次ゲートまでの距離) から出せる
- オフスクリーンマーカーは、必要なら「後ろにいるときだけ小さく出す」形で後から足す

#### モード構成
- **タイトル画面で VS / SOLO を選べるようにする**。ライバル機を出すかどうかのフラグ 1 個で切り替わる
  - ゴースト方式にしたことで、ソロと VS の差が「ライバルを出すか」だけになった
  - 2 シーンに分けると HUD の修正を 2 箇所に入れ続けることになるので分けない
- 今の `play_fly.gd` (ソロ画面) を **4 フェーズ構成 (タイトル / カウントダウン / プレイ中 / 結果)
  に書き直す**。削除ではなく、今のソロの挙動を SOLO モードとして内包する
- **`G` キーの巡回先は「Ball Collector ⇄ Fly By」の 2 つのまま**。
  4 モードを 1 キーで回すと目的の画面にたどり着くのが面倒になる

#### 墜落したときの復帰
- **VS のときだけ「直前に通過したゲートの手前へ復帰」にする** (`@export var respawn_at_gate`)。
  ソロは今のまま (コースを作り直してスタートへ戻る)
  - コースを作り直すと人間のコースだけ別形状になり、ゴーストと経路が食い違って破綻する
  - ソロを変えると **学習済み方策が訓練された環境と変わり、README とレポートに載せた
    39.6 個という実測値も測り直しになる**。フラグ 1 個で分ければソロは 1 ビットも変わらない
- 両者のコースを同時に作り直す案は却下。**わざと墜落して相手を引き戻す**遊び方が成立してしまう
- 残る割り切り: VS では埋め込み方策が「学習時と少し違うルール」で動く。
  全力の AI はほとんど墜落しないので実害は小さく、影響はベンチで実測して確認する

#### 演出
- **`fx.gd` / `sfx.gd` を移植し、ドローン用に調整する**
- **落下影は演出ではなく機能**。今の Fly By で高度を判断する手がかりは床のグリッドだけで、
  追従カメラでは「このまま降りたら地面に当たるか」が読みにくい。
  機体の真下に影を落とせば高度と水平位置が一目で分かる
- ただし `fx.gd` の落下影は `FLOOR_Y := 0.0` で固定サイズ。ドローンは高度 8〜28m を飛ぶので、
  **高度に応じて薄く・大きくする**調整が要る。そうしないと高高度でも影が濃く、距離感を誤らせる
- トレイルの長さを速度に連動させる。スロットルが操作要素になったので、
  「今どれくらい出ているか」を数字を読まずに伝えられる
- `sfx.gd` はほぼそのまま使える (pickup → ゲート通過、rival → AI が通過、fall → 墜落、
  beep / go / win / lose はそのまま)
- 新規のシェーダやパーティクルには手を出さない。llvmpipe (CPU 描画) で重くなる

#### 完了条件
- **実装 ＋ ベンチでの実測までを一区切り**とし、遊んでレベル設定が妥当だと確認できた時点で
  README とレポート PDF を更新する。実測値が出るまでドキュメントには書けることが無い
- Windows ネイティブビルドは見送り。テンプレートさえあれば 1 分で出せるので必要になってから

### 再利用できるもの

| 対象 | 扱い |
|---|---|
| `tools/export_policy.py` | **変更不要**。形状をモデルから読むので 17→64→64→3 でもそのまま動く (5,507 個になる見込み) |
| `game/scripts/policy.gd` | 出力が `Vector2` 決め打ち。**3 次元化が要る** |
| `game/scripts/fx.gd` | 移植して落下影を高度対応に |
| `game/scripts/sfx.gd` | ほぼそのまま |
| `game/scripts/bench.gd` | ドローン用に書き換え |
| `game/scripts/screenshot.gd` | そのまま |

### マージで衝突するファイル

ブランチは今回 Fly By 実装で書き換えたのと同じファイルを触っている。

- `game/scripts/world_view.gd` — **`follow()` のシグネチャが変わっている**
  (`Node3D` を渡す形 → 座標 ＋ spread ＋ shake を渡す形)。ここがいちばん重い
- `game/scripts/play.gd` / `arena.gd` / `player.gd` / `boot.gd` / `train.gd`
- `README.md` / `spec.md` (このファイル自身)

### 検証の方法 (再学習は不要)

**既存の `models/fly_by.zip` の重みを埋め込むだけなので、この作業に学習は一切要らない。**
いちばん重い工程でも Godot ヘッドレス 1 プロセス、コア 1 つぶん。PC はほとんど発熱しない。

1. `export_policy.py` で重みを書き出し、`--policy-probe` で **Python と GDScript の出力が
   float32 の丸め誤差まで一致すること**を確認する (クリップ前の生の値で比較する。
   飽和で差が隠れないように)
2. ベンチで 4 レベルそれぞれのスコアを実測する
   - **`physics_ticks_per_second` と `time_scale` を同時に上げれば挙動を変えずに早送りできる**
     (`sync.gd` と同じ手法。1 tick あたりの時間が 1/60 秒のまま保たれる)。
     Ball Collector のベンチは `time_scale` だけを上げて物理が変わり、等速固定になっていた
   - 4 倍速なら 4 レベル × 5 レースが約 5 分、1 コアだけ
3. xvfb で PNG を撮って見た目を確認する
4. ブラウザで実際に遊び、レベルの手応えを確認する

### 判断の根拠にした実測値

| 項目 | 値 |
|---|---|
| 学習済み AI (全力、決定論的) | 39.6 個 / 60 秒 (最高 42) |
| 手書きの貪欲方策 (減速あり) | 34.4 |
| 手書きの貪欲方策 (全開) | 33.0 |
| 学習前 (ランダム) | 0.0 |
| 理論上限 (60 秒 × 最高速 18 m/s ÷ 平均間隔 21.5m) | 約 50。曲がるぶんを引くと 40 前後 |

**全力 39.6 と貪欲 34.4 の差が小さい**ので、手加減の幅は速度上限で下方向に広げる必要がある。
速度上限 6 / 9 / 12 / 18 で概ね 13 / 20 / 27 / 39.6 になる見込み (要実測)。

### 意図的に見送ったこと

- 機体同士の衝突判定 (同一経路を飛ぶのでぶつかり続けて事故ゲームになる)
- 相手を観測に入れた再学習 (今の AI は相手が見えていない。妨害も回避もしない)
- ドローン固有の新規演出 (スピードライン等)。トレイルの速度連動で足りる
- Windows ネイティブビルド

## 実装した結果 (2026 年 8 月 19 日)

計画どおり実装し、動作を確認した。**再学習は一度も行っていない** (既存の重みの埋め込みのみ)。

### 実測値

| 項目 | 結果 |
|---|---|
| 埋め込み推論と Python の一致 | Fly By は完全一致、Ball Collector は float32 の丸め誤差のみ |
| ゲーム内 AI (全力) のスコア | 39.25 個 / 60 秒 (Python 推論は 39.6) |
| 手加減 4 段階 | EASY 15.4 / NORMAL 22.5 / HARD 28.9 / FULL POWER 39.3 |
| 手加減の設定値 | 速度上限 6 / 9 / 12 / 無制限、行動ノイズ 0.35 / 0.20 / 0.10 / 0 |
| 貪欲方策 (回帰確認) | 35.3 (改修前 34.4。誤差の範囲で環境は変わっていない) |
| 重みのサイズ | Fly By 5,507 個 (31 KB) / Ball Collector 5,122 個 (29 KB) |

ベンチは `physics_ticks_per_second` と `time_scale` を同時に上げる方式で 4 倍速にできた。
1 プロセスで 8 レースが約 30 秒。

### 計画から変えたこと

- **落下影 → 対地マーカー**。計画では「機体の真下に影を落とす」としていたが、
  実装して確認したところ、追従カメラでは**機体の真下の地面が画面の下端より外に来て見えなかった**。
  そこで進行方向の先に投影し、機体色の加算描画にした。
  距離が高度を表し、同時に「このまま行くとどこに着くか」も分かる。
  暗い影のままでは暗い地面に埋もれて見えなかったのも理由。

### 実装中に見つけた罠

- **コース生成のシードを固定すると学習が壊れる**。VS モードのために `rng_seed` を足したが、
  既定値 0 のままだと全 16 コースが毎エピソード同じ形になり、学習が偏る。
  `0 = 毎回ランダム` という規約にして回避した。VS のときだけ明示的に同じ値を入れる。
- **演出のサイズは俯瞰カメラ前提だった**。Ball Collector は 20m 上空から見ているが
  Fly By は機体の 10m 後ろなので、"+1" の吹き出しと破片が画面を埋め尽くした。
  `Fx.burst()` にサイズ倍率を足し、Fly By では 0.4 倍にした。
- **トレイルを機体の位置に置くと機体に白い板が重なる**。進行方向の 1.8m 後ろに置いてずらした。
